# 项目文档与架构整理 需求文档

**创建日期：** 2026-05-14
**项目：** knowwind-wx
**功能模块：** 项目文档与架构整理

## 一、需求概述

项目在 2026-05-09 完成了从 shell 脚本 + CLI 工作流到 FastAPI 服务的架构迁移，但文档和部分代码未同步更新，造成严重不一致。本次整理的目标是：**让代码、文档、安装脚本三者保持一致**，消除旧架构的所有残留。

## 二、不一致清单

| 文件 | 问题 |
|------|------|
| `agent/` 目录 | 旧 CLI 工作流残留（bind/run/status，对接 /api/ingest 旧接口） |
| `knowwind-wx` 入口 | 指向已删除的 agent.cli |
| `AGENTS.md` | 描述旧 shell 脚本架构（config.sh、export-wechat-groups.sh 等） |
| `README.md` | 安装方式为"让 AI Agent 安装"，与 install.sh curl 安装不符 |
| `docs/TECH.md` | 目录结构含不存在文件，数据流标题引用旧脚本 |
| `scripts/push-insights.py` | 仍在解析已不存在的 config.sh |
| `install.sh` | 未安装 fastapi/uvicorn，完成提示仍有旧 bind 命令 |

## 三、整理范围

### 删除
- `agent/` 目录（cli.py、collector.py、config.py、__init__.py）
- 旧 `knowwind-wx` 入口文件

### 修复代码
- `scripts/push-insights.py`：config.sh 解析改为 dotenv

### 重写/更新文档
- `AGENTS.md`：完全对应 FastAPI + .env 架构
- `README.md`：安装改为 `curl | sh`，配置改 `.env`
- `docs/TECH.md`：目录结构、数据流步骤标题

### 同步 install.sh
- 新增 fastapi、uvicorn 依赖安装
- 完成提示改为：编辑 .env → `knowwind-wx run`

### 新建 knowwind-wx 入口
- `run`：启动 uvicorn FastAPI 服务
- `status`：检查服务运行状态

## 四、验证标准

- 61 项自动化检查全部通过
- 无对已删除 agent/ 的残留引用
- README / AGENTS.md / install.sh / TECH.md 描述一致
